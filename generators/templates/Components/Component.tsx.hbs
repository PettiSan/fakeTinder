import React from 'react'

export type {{pascalCase name}}Props = {
  onClick?: () => void
  title?: string
}

export const {{pascalCase name}} = (props: {{pascalCase name}}Props) => {
  return (
    <div className="py-10">
      <div className="font-bold font-[2rem]">
        {props.title || '{{pascalCase name}} component'}
      </div>
      <button
        onClick={props.onClick || (() => {})}
        className="flex items-center  space-x-2 border p-2 rounded-[6px] mt-2 border-[#000]"
      >
        <span>The cake is a lie</span>
      </button>
    </div>
  )
}
