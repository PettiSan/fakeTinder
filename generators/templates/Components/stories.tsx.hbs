import { Meta, Story } from '@storybook/react'
import React from 'react'
import { {{pascalCase name}} as Component, {{pascalCase name}}Props } from './{{pascalCase name}}'

export default {
  title: 'Components/{{pascalCase name}}',
  component: Component,
} as Meta

const Template: Story<{{pascalCase name}}Props> = (args) => <Component {...args} />

export const {{pascalCase name}} = Template.bind({})

{{pascalCase name}}.args = {
  title: 'Edit me',
}
